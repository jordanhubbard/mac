---
id: task_cb6aee7dc56741cd89489b2aed672820
status: open
deps: []
links: []
created: 2026-06-14T20:02:45.525322+00:00
type: task
priority: 0
mac-task-id: task_cb6aee7dc56741cd89489b2aed672820
---
# OpenShell: fold enforcement bootstrap into deploy-mac-fleet.sh (standard for new nodes)

Fold the (now proven) OpenShell sandbox-enforcement bootstrap into deploy-mac-fleet.sh so any new node comes up enforcing + fail-closed automatically ("standard for new nodes").

Proven across the fleet this session (rocky x86 / bullwinkle x86 GPU / natasha ARM64 GPU). Durable steps to encode (all recorded as evidence on task_9babe9013f134283b32eda92bd2bfa90):
1. Install: openshell CLI (uv tool / pip) + per-arch openshell-gateway from the NVIDIA release asset (openshell-gateway-<arch>-unknown-linux-gnu.tar.gz; NOT a source build).
2. Build/load mac-hermes image: COMMIT the proven Containerfile (python:3.12-slim multi-arch + pip install mac + zz_hermes_vendor.pth so hermes_cli imports + chown sandbox:sandbox /sandbox for the docker driver + iproute2/git/curl + sandbox user). Native build per-arch (x86 + arm64).
3. CDI for GPU hosts: regenerate the spec to match the current driver (sudo nvidia-ctk cdi generate); pick the driver — podman (>=5.x, CDI ok) or docker (when podman is too old for the nvidia-ctk CDI version, e.g. natasha's podman 4.9.3).
4. Gateway: gateway.toml (podman OR docker driver), generate-certs (JWT), systemd --user service, podman.socket+linger (podman driver), `openshell gateway add http://127.0.0.1:17670`+select, firewall :17670 on the public/LAN NIC (persistent systemd oneshot).
5. Render policy (image_runtime=/opt/mac-venv; egress to the node's hub — local host.openshell.internal OR remote tailscale IP).
6. mac source must include openshell_runtime.py (#154/#155 are merged); MAC_OPENSHELL_BIN must be an ABSOLUTE path; MAC_OPENSHELL_CREATE_ARGS must be QUOTED (mac.env is shell-sourced) and include --gpu on GPU hosts + the rewritten-config upload + HOME=/tmp.
7. Flip: MAC_OPENSHELL_SANDBOX=1, validate a real task, then MAC_ALLOW_UNSANDBOXED_YOLO=0.

Also: commit the ARM/x86 mac-hermes Containerfile to deploy/openshell/ (there is currently no committed build recipe).
