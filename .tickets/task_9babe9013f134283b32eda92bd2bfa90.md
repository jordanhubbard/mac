---
id: task_9babe9013f134283b32eda92bd2bfa90
status: open
deps: []
links: []
created: 2026-06-14T06:13:39.866703+00:00
type: task
priority: 0
mac-task-id: task_9babe9013f134283b32eda92bd2bfa90
---
# Activate OpenShell executor enforcement (de-risked; blocker: sandbox JWT auth)

Activate OpenShell executor enforcement — de-risked; one blocker left (sandbox JWT auth)

GOAL: run the task executor's --yolo Hermes under an OpenShell sandbox so the
sandbox is the guardrail (then MAC_OPENSHELL_SANDBOX=1 + MAC_ALLOW_UNSANDBOXED_YOLO=0).

WHAT'S ALREADY DONE / PROVEN (rocky / do-host1):
- Executor seam is complete (task_executor._maybe_wrap_openshell, policy
  resolution, --env cred forwarding; rocky/natasha/bullwinkle hard-coded as
  sandbox-required). Policy renderer shipped: `mac openshell render-policy`.
- openshell is a uv tool; sibling daemon `openshell-gateway`. Runtime image
  ALREADY built + good: localhost/mac-hermes:latest (python3.12 + mac at
  /opt/mac-venv). OpenShell supervisor image pulled.
- Gateway runs with: `openshell-gateway --disable-tls --drivers podman`
  (needs the rootless podman API socket: `loginctl enable-linger jkh` +
  `systemctl --user enable --now podman.socket`; XDG_RUNTIME_DIR=/run/user/1001).
  A user systemd unit ~/.config/systemd/user/openshell-gateway.service exists
  (currently DISABLED + bound 127.0.0.1 for safety).
- HOST<->SANDBOX NETWORKING SOLVED: bind the gateway to a routable host IP
  (0.0.0.0 / 100.125.137.89) and the rootless-podman sandbox container reaches
  it (verified `</dev/tcp/100.125.137.89/17670>` REACHABLE from a mac-hermes
  container). Register the CLI endpoint with
  `openshell gateway add http://100.125.137.89:17670 --local`.

THE ONE REMAINING BLOCKER:
- The sandbox supervisor needs a JWT to fetch its policy/push logs:
  "Policy fetch failed ... no sandbox token source available — set one of
  OPENSHELL_SANDBOX_TOKEN / _TOKEN_FILE / OPENSHELL_K8S_SA_TOKEN_FILE".
- With TLS/auth disabled the gateway issues NO sandbox token. 0.0.59 exposes
  only mTLS + OIDC as CLI flags; sandbox-token signing is via the RFC-0003
  --config TOML or the `openshell-gateway generate-certs` mTLS path.
- NEXT STEP: configure gateway auth so it issues sandbox tokens — either set up
  mTLS (generate-certs + --tls-cert/--tls-key/--tls-client-ca, sandbox gets a
  client identity) OR the sandbox-token signing block in the RFC-0003 config.
  Needs the OpenShell 0.0.59 config schema / upstream docs (don't guess on prod).

ROLLOUT AFTER UNBLOCK (staged, per operator):
1. Gateway service ENABLED (durable) on rocky; validate a sandboxed `echo` then a
   real task; confirm an off-policy egress is DENIED (the guardrail proof).
2. Render+install policy (`mac openshell render-policy --into ~/.mac/openshell-policy.yaml`);
   set MAC_OPENSHELL_CREATE_ARGS=--from localhost/mac-hermes:latest.
3. MAC_OPENSHELL_SANDBOX=1 on rocky; validate a real task end-to-end.
4. Roll to natasha/bullwinkle (GPU: build the image there + likely --gpu); then
   MAC_ALLOW_UNSANDBOXED_YOLO=0 (fail-closed). Make gateway service + flags
   durable in deploy/deploy-mac-fleet.sh.

STATE: rocky reverted to pre-work (gateway disabled, MAC_OPENSHELL_SANDBOX unset,
agent healthy). No regression. linger + podman.socket left enabled (benign).
