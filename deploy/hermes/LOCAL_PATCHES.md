# Local delta of the vendored Hermes tree (`src/mac/_hermes`)

`src/mac/_hermes` is a pinned snapshot of upstream (see
`../../src/mac/_hermes/SNAPSHOT_PIN`). `scripts/vendor-hermes-snapshot.sh`
reproduces it from **pristine upstream at the pinned commit** via three deltas,
which together were validated to reproduce the committed tree **byte-for-byte**
(a fresh pristine clone → all patches → prune → copy INCLUDE → remove dashboard
bundles → copy overlay → `diff` clean):

1. **Patches** — `deploy/hermes/*.patch`, `git apply`-ed to the upstream tree in
   alphabetical order. These are text-source edits to files that survive into
   the vendored surface, so they ride on top of upstream and conflict loudly if
   upstream moves.
2. **Removals** — the pipeline deletes `plugins/*/dashboard/dist` (built
   dashboard bundles that MAC does not vendor; upstream ships them, so a
   re-vendor would otherwise reintroduce them).
3. **Overlay** — `deploy/hermes/overlay/` holds MAC-**authored** files that are
   not part of the upstream surface. `rm -rf "$DEST"` at the start of a re-vendor
   drops them, so they are copied back in after the upstream copy.

`tests/test_hermes_vendor_integrity.py` pins a content digest of the tree so any
future drift — a hand edit or a re-vendor — fails loudly rather than silently.

### The reproduction claim is now checked, not just asserted

The digest test only checks the **output**. It cannot see a broken **input**,
and for months the input was broken: `fts5-orphan-schema-recovery.patch` was
malformed (a hand-edited diff whose `@@` line counts no longer matched its
body), and `remove-duplicate-top-level-skills.patch` carried MAC-repo paths
(`src/mac/_hermes/tools/skills_sync.py`) because it had been generated by
diffing the vendored tree rather than upstream. `git apply` runs under
`set -euo pipefail`, so `--apply` aborted before writing anything and the
byte-for-byte guarantee above was simply false. Nothing failed, because nothing
ran it.

Both patches were regenerated from real diffs against pristine upstream at the
pin, and two guards now stand behind the claim:

* **`tests/test_hermes_patch_set_is_appliable.py`** — offline. Verifies every
  hunk header's line counts match its body, and that no patch targets
  `src/mac/_hermes/...`. Runs in every build.
* **The `hermes-revendor` CI job** — shallow-fetches the pin, runs the real
  re-vendor, and fails if the result differs from the committed tree.

**Never hand-edit a patch's `@@` header, and never generate a patch by diffing
`src/mac/_hermes`.** Regenerate against a pristine upstream checkout at the pin.
Those two mistakes are precisely what the guards above catch.

## Patch set

| Patch | Purpose |
|-------|---------|
| `disable-shutdown-chat-notices.patch` | (pre-existing) |
| `mac-provider-decision.patch` | (pre-existing) |
| `fts5-orphan-schema-recovery.patch` | Makes `SessionDB._init_schema()` in `hermes_state.py` self-heal an orphaned FTS5 schema — a `messages_fts` / `messages_fts_trigram` virtual-table declaration whose backing shadow tables (`*_data`/`*_idx`/`*_docsize`/`*_config`) were lost or corrupted. The startup probe uses the FTS5 `integrity-check` command because metadata-only `SELECT ... LIMIT 0` can miss a partial orphan on macOS SQLite. The fix classifies both platform-specific missing-shadow errors and Linux SQLite's table-scoped `fts5: corruption found` signature, drops the orphaned vtable + triggers + stray shadow tables (bumping the schema cookie so the live connection reparses), recreates the index from the canonical DDL, and backfills from `messages`. Touches only `hermes_state.py`; applies independently of the other patches. |
| `mac-runtime-context-prompt.patch` | (pre-existing) |
| `multi-slack-mvp.patch` | True multi-workspace Socket Mode. Reads `~/.hermes/slack_accounts.json` — a JSON array of `{name, bot_token, app_token}` — and gives each account its own `AsyncApp`, `AsyncSocketModeHandler` and websocket, keyed by account name in `self._apps` / `self._handlers` / `self._socket_mode_tasks`. Flat `SLACK_BOT_TOKEN` / `SLACK_APP_TOKEN` remain the single-account fallback used only when that file is absent; an account missing either token is skipped with a warning. Each app is built with `request_verification_enabled=False`: Socket Mode delivers events over an app-token-authenticated websocket, so there is no inbound HTTP request to verify and no signing secret to verify it with, and `slack_bolt` >= 1.27 otherwise raises `signing_secret must not be empty` at construction — before any connection is attempted. Tests: `tests/test_hermes_slack_socket_mode_construction.py`. |
| `post-snapshot-mac-fixes.patch` | **reconstructed** — the 10 text-source edits that had been made directly to the vendored tree without a patch (X-MAC billing-attribution headers in `agent/agent_init.py`, the Slack thread-trigger fix, reconciliation edits, and other de-personalization/runtime edits across `agent/`, `gateway/`, `cron/scheduler.py`, `tools/{code_execution,todo}_tool.py`, `toolsets.py`). Applies after `multi-slack-mvp.patch` (both touch `gateway/platforms/slack.py`). |
| `review-completion-exit-status.patch` | Makes quiet one-shot Hermes runs return nonzero when a turn is partial, failed, or incomplete (including max-turn exhaustion), so MAC cannot mistake a generated exhaustion summary for completed reviewer work. |
| `sandbox-path-precedence.patch` | Reasserts MAC's task-toolchain-first, image-runtime-second PATH after Hermes login profiles run, so terminal snapshots and fallback commands cannot select host or profile-provided binaries ahead of sandbox contract tools. |
| `remove-duplicate-top-level-skills.patch` | Adds `remove_shadowed_top_level_duplicates()` to `tools/skills_sync.py` and integrates it into `sync_skills()`. Archives legacy top-level skill dirs (e.g. `~/.hermes/skills/writing-plans/`) that are shadowed by a categorized copy (e.g. `~/.hermes/skills/software-development/writing-plans/`), eliminating "Ambiguous skill name" errors when `skill_view()` is called by bare name. |
| `oneshot-skip-memory-context.patch` | Passes `skip_memory=True` and `skip_context_files=True` to the `AIAgent` constructed in `hermes_cli/oneshot.py` `_run_agent()`, so `hermes -z` one-shot runs do not inherit persistent memory (`MEMORY.md`/`USER.md`) or repo context files from the shared `HERMES_HOME`. Applies independently of the other patches (only hunk is in `hermes_cli/oneshot.py`). Tests: `tests/test_hermes_oneshot_session_isolation.py`. |
| `show-reasoning-per-channel.patch` | Adds a per-channel tier (`display.channels.<platform:chat_id>.*`) above the per-platform tier in `gateway/display_config.py` (`channel_config_key()` helper; thread→parent-channel fallback), and makes `/reasoning show\|hide` write the CURRENT channel's override by default (`--global` keeps the old platform-wide write). Lets one room opt into seeing agent reasoning without enabling it platform-wide. Sorts after the other `gateway/run.py` patches because its hunks are diffed against the fully-patched tree. Tests: `tests/test_gateway_display_channel_overrides.py`. |
| `zz-public-api-docstrings.patch` | Adds concise one-line PEP 257 docstrings to module-level public functions that were missing them under `agent/` (`account_usage.py`, `agent_runtime_helpers.py`, `context_references.py`, `credential_pool.py`, `credential_sources.py`, `curator.py`, `curator_backup.py`, `gemini_native_adapter.py`, `onboarding.py`, `usage_pricing.py`) and the root `cli.py` module. Documentation-only: no behavior or signature changes, no inline comments. Sorts last (`zz-`) so it applies against the fully-patched tree. |

## Overlay files (`deploy/hermes/overlay/`)

MAC-authored, not in upstream — dropped by `rm -rf "$DEST"` on re-vendor unless
copied back:

- `README.md` (the "this tree is vendored, re-vendor with the script" doc)
- `plugins/image_gen/mac-hub/{__init__.py,plugin.yaml}` (the mac-hub image-gen plugin)
- `plugins/image_gen/nvidia/{__init__.py,plugin.yaml}` (the NVIDIA image-gen plugin)
- `tools/fleet_tool.py`, `tools/embedding_tool.py` (MAC agent tools)

## How the patch was reconstructed and validated

The reconstruction diffed the **real pipeline output** (pristine@pin + the
existing patches, pruned to the vendored surface) against the committed tree —
the initial-snapshot commit turned out **not** to be byte-identical to
pristine+patches (de-personalization altered more files than the post-snapshot
commits), so an earlier attempt that diffed against the snapshot commit was
wrong and was discarded. `.vendor-check.sh` (temporary; not committed) performed
the reconstruction and the end-to-end validation against a fresh network clone
of pristine upstream. It printed `IDENTICAL`, i.e. the three deltas fully
reproduce the committed `_hermes`.

Before the next pin bump, re-run `scripts/vendor-hermes-snapshot.sh --apply` end
to end and confirm the resulting tree still matches the digest in
`tests/test_hermes_vendor_integrity.py` (it will fail loudly if not).
