diff --git a/cli.py b/cli.py
index baf0339..44e6c54 100644
--- a/cli.py
+++ b/cli.py
@@ -15074,6 +15074,19 @@ class HermesCLI:
 # Main Entry Point
 # ============================================================================
 
+def _quiet_result_exit_code(result: object) -> int:
+    """Return a fail-closed exit code for machine-readable one-shot runs."""
+    if not isinstance(result, dict):
+        return 0
+    if result.get("failed") or result.get("partial"):
+        return 1
+    # Iteration exhaustion produces a summary string but completed=False. It
+    # is not successful automation merely because the provider returned text.
+    if result.get("completed") is False:
+        return 1
+    return 0
+
+
 def main(
     query: str = None,
     q: str = None,
@@ -15475,7 +15488,7 @@ def main(
                     print(f"\nsession_id: {cli.session_id}", file=sys.stderr)
                     
                     # Ensure proper exit code for automation wrappers
-                    sys.exit(1 if isinstance(result, dict) and result.get("failed") else 0)
+                    sys.exit(_quiet_result_exit_code(result))
             
             # Exit with error code if credentials or agent init fails
             sys.exit(1)
