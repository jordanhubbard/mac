{
  "name": "mac-ide",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MAC control plane as an IDE — VS Code/Cursor-style shell over the fleet (file/task tree, Monaco editor, activity/evidence panel, Agents panel).",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "package": "npm run build && mkdir -p ../dist && tar -czf ../dist/mac-ide-web.tar.gz -C dist .",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@monaco-editor/react": "^4.6.0",
    "allotment": "^1.20.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
