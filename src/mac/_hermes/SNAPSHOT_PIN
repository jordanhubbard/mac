upstream https://github.com/NousResearch/hermes-agent.git
commit b1a25404b638bfbd79ce4d08b49afc0ee1361528
vendored 2026-05-31T03:16:29Z

# NOTE: this pin records the upstream BASE the tree was vendored from, NOT the
# current state of src/mac/_hermes. The tree has MAC-local edits made directly
# after the snapshot; these are now captured as
# deploy/hermes/post-snapshot-mac-fixes.patch (see deploy/hermes/LOCAL_PATCHES.md),
# so the vendor script re-applies them and re-vendoring is idempotent again.
# tests/test_hermes_vendor_integrity.py pins a content digest so any drift (a
# hand edit or a re-vendor) fails loudly instead of silently.
